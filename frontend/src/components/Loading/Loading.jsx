import { useNavigate } from "react-router-dom";
import {Box, Text, Wrapper} from "./Loading.styles";


const Loading = () => {
  const navigate = useNavigate();

  return (
    <Wrapper>

    </Wrapper>

  );
}

export default Loading